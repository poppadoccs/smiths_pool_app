# Optional Q109 Re-Inspection Summary

## Agreed scope

Cassandra requested optional #109 on July 21, 2026 and confirmed July 22:
an optional section at the end of the form, allowing attached photos.
On September 10 she clarified that it must be exactly like #107, renamed
"Re-Inspection Summary". Reuse the existing structured summary editor.

## Implementation

1. Append one stable optional textarea to the verified Pool/Spa template.
   Preserve every existing field ID, value, order and label. Reuse the
   Q107 summary editor, with eight photos per bullet and 30 per section.
2. Persist Q107 and Q109 in separate reserved summary keys. Show bullets/photos in
   PDF and email. Omit an unused Q109 from reports; honor PDF exclusions.
3. Supply a read-only-by-default template update with a backup and a
   compare-and-swap write. Do not touch production before validation.

## Verification and release

- Test template idempotence/conflicts, optional submission, independent Q107/Q109
  persistence, bullet reorder/removal, photo cleanup, PDF and email parity.
- Run the existing test suite, TypeScript checking, and production build.
- Inspect a generated PDF and exercise the section's UI.
- Publish only after the checks pass; then inspect the live template,
  apply the targeted update, and smoke test a clearly marked test job.
- Never send a test email to the client without explicit authorization.

## Access

GitHub main confirmed at ba8010a9fdd533a34817b283e01c3d0246beebcd.
Neon tools became available after the revised local checks. Live database
verification is a release prerequisite; do not claim a live test passed
until it is actually performed.
