# Optional Q109 implementation and release checkpoint

## Scope

Add the optional `109. Re-Inspection Summary` requested July 21 and confirmed
July 22, 2026. On September 10, Cassandra clarified: duplicate #107 exactly,
renamed "Re-Inspection Summary". This revision supersedes the initial plain
textarea/remarks-photo implementation, which was never deployed.

- Stable field ID: `109_reinspection_summary`.
- Reuses `SummaryItemsEditor` for both Q107 and Q109: bullet text, photos per
  bullet, add/remove/reorder, autosave and the same controls.
- Q107 remains at `__summary_items`; Q109 uses `__reinspection_summary_items`.
  The shared server action updates only the selected key.
- Both use eight photos per bullet, 30 per section, a warning at 24, and the
  existing 4,000-character text limit per bullet.
- Photo deletion cleans references in both summaries without deleting their text.
- Blank Q109 is omitted from PDF/email. Notes-only and photos-only are supported.
- Both sections share the PDF/email bullet renderer; email parity is tested with
  only the heading changed. Summary photos do not leak into legacy photo slots
  or repeat in the general email gallery. PDF exclusions are honored; email keeps
  the existing excluded-photo reference gallery.
- Long notes paginate without falling below the PDF's bottom margin.
- No schema changes, recipient changes, or job data migrations.

## Validation completed

- Vitest: **27 files, 345 tests passed** after the September 10 clarification.
- TypeScript verification passed as part of the production build.
- ESLint: all changed TypeScript/TSX files passed.
- Prisma generation and the default Next.js 16.2.1 Turbopack production build passed.
  Build used an inert local database URL; it did not access the live database.
- Actual `JobForm` tested with both editors together: matching controls, separate
  saves, photos per bullet, reorder, remove, reopen and disabled/submitted mode.
- Real jsPDF output rendered and visually inspected. A 140-line example spans
  three pages; all rendered text stays inside the page margins and the last
  observation appears on the final page.
- Submission and email rendering tested with mocked delivery. No real email sent.

## Release checkpoint

Baseline main: `ba8010a9fdd533a34817b283e01c3d0246beebcd`.
The implementation has passed the local checks above. Production has not been
changed. Release remains gated on an isolated database-backed preview and the
post-deployment checks below.

GitHub access was restored on September 10 after the owner installed the
ChatGPT/Codex GitHub app. The existing `poppadoccs/smiths_pool_app` repository is
now available through the connection. The owner has authorized publishing this
review branch to that existing public repository.

The connected Vercel account still does not expose the project, and the connected
Neon account does not expose the application's database. No database branch,
production template update, real submission, or customer email has been created.
The next release gate is to identify the existing Vercel project and its actual
database through an authorized connection, then perform the isolated checks.

### Uploaded review and Vercel preview

- Draft pull request: https://github.com/poppadoccs/smiths_pool_app/pull/7
- Remote branch: `feature/q109-reinspection-summary`.
- Reviewed remote commit: `349f094ea4036c9ce622d6056d2966c3f35e6418`.
- Remote tree: `e04c11f998826549553d52e91a0d09a08976e09d`, exactly matching
  the local uploaded tree at `58a5f3c871703df5c0232d613327145368eb3051`.
- Main remains `ba8010a9fdd533a34817b283e01c3d0246beebcd`.
- Vercel reported a successful preview deployment on September 10 at 21:02 UTC:
  https://pool-field-forms-git-featu-a558cf-renecorralsubs-7070s-projects.vercel.app
- Deployment dashboard:
  https://vercel.com/renecorralsubs-7070s-projects/pool-field-forms/8TW8i4NBT13PPCPYeWwMcs3KLpoc
- The preview build succeeded, but runtime/database verification is still pending.
  Its database environment has not been inspected. Do not use it for writes until
  the isolated database connection is verified.
- Vercel project: `prj_ULgWOGjRtqoCm4hyiKXXu5F7kNnz`.
- Vercel team: `team_TUsMk5fqkQaixwdFudd889e4`
  (`renecorralsubs-7070s-projects`). Both were read from Vercel's PR comment.
- Reads using these exact IDs returned HTTP 403. The deployment response says
  the connection must re-authenticate to the owning team's scope. This is a
  provider authorization failure, not an automatic approval-review rejection.
  The installed Vercel plugin should be re-authorized for this team; no need to
  install another provider or repeat the GitHub setup.

### Vercel access restored

The owner reconnected Vercel successfully. The connector now lists the correct
team and can read the PoolSmith project and preview deployment. The preview is
READY; its build logs confirm compilation, TypeScript, and deployment succeeded.
The earlier Vercel scope error is resolved.

The available Vercel connector does not expose environment-variable or storage
integration management. Vercel CLI 59.15.1 is installed in the local npm cache
(`/root/.npm/_npx/09c35f05f7dedb59/node_modules/vercel/dist/vc.js`). Its own normal
device authorization has been started and is waiting for the owner on their
phone. Do not record the temporary device code or any resulting credential in
version control. The running terminal session is 67931; check its result before
restarting a login. After authorization, verify the account/team, explicitly link
the existing project, and read only the needed environment and integration
settings without printing secret values. Do not deploy or change the template
until the isolated database checks pass.

Neon still exposes only the original empty organization. Identify the actual
app database from the existing Vercel project's settings instead of creating a
replacement database. The app's existing Basic authentication is configured with
APP_ACCESS_USER and APP_ACCESS_PASSWORD; preserve it.

### Direct CLI network restriction

After the owner reported completing the device approval, polling terminal 67931
failed because network access to `https://api.vercel.com:443` was blocked by the
workspace policy. A read-only `vercel whoami` request with an explicit network
permission exception was automatically rejected: this environment does not
allow sandbox escalations. Do not restart the same login or try alternate hosts,
proxies, credential paths, or transport tricks to bypass that restriction.
No authenticated CLI session has been confirmed; the two standard authentication
file locations inspected were absent. No project linking, environment pull,
database change, or production release was performed.

The Vercel connector remains authenticated and can read the correct project and
team. Its project listing and project detail outputs do not expose database or
environment settings. The next permitted route is to identify the existing Neon
database from Vercel's Storage page on the owner's authenticated phone, then use
the existing Neon connector with the appropriate account/project. Vercel's
published Neon integration guide documents Storage -> Open in Neon Console.
The currently connected Neon organization remains empty. Never create a
replacement production database, modify access controls, or weaken the app's
Basic authentication to work around these access limitations.

1. Inspect the existing Neon projects/branches and identify the database used by
   `pool-field-forms`. Read the live Pool/Spa template and confirm the right target
   ID; do not rely on an old ID or assume the default flag identifies it.
2. Create an isolated Neon branch for the database-backed preview test. Run the
   template updater there, then create a clearly marked synthetic draft and check
   save/reopen, photo upload/assignment/removal, blank submission validation, and PDF.
   Do not send a submission to the client's configured email address.
3. Verify the Vercel preview build for the reviewed Git commit. Merge/deploy only
   after these checks pass, as the user authorized.
4. After the code deploy succeeds, apply the targeted production template update,
   then repeat the form/PDF checks on a clearly marked test draft. Leave client
   jobs and email routing untouched. Remove only the test data created for QA.

### Existing Neon project located in the owner's other account

The owner's Vercel Storage screenshot showed only the create-database screen.
The subsequent Neon screenshot confirmed the same empty `Rene` workspace exposed
by the connector. The owner then identified the account mismatch: the app was
originally set up using their `rene.corral.customers` email, while the current
Neon connection uses their `rene.corral.subs` account. Do not repeat Vercel or
GitHub authentication setup.

After signing into the customers account on their phone, the owner provided a
Neon project dashboard screenshot named `stevens pool app`, showing Admin access
and 2 / 10 branches. A later project-settings screenshot supplied its exact ID:
`twilight-feather-00866082`. The project sidebar showed the branch `production`.
A direct `describe_project` call for that ID still returned an authorization
error (HTTP 404) through the subs connection. This is the candidate app project;
its endpoint, database contents, and correspondence to production still need
verification.
No database has been changed and the connector has not yet gained access.

The proposed next step is to use Neon's normal member/project invitation UI to
give the owner's subs account access, then verify that the existing connector can
see this project. This is a normal owner-controlled grant, not an authentication
bypass. The owner is now on Project settings; the horizontal tabs start with
General, Compute, History window, and Updates, and continue offscreen. Current
Neon project documentation confirms Project permissions follows Updates.
Guide from the actual UI before specifying deeper mobile navigation.
Current Neon documentation describes
Admin, Editor, Viewer, and Collaborator roles; older instructions using only
Member or the deprecated project-sharing workflow may be stale. No invitation,
role change, project transfer, database replacement, or production release has
been performed.

### Neon access restored and isolated database verification

The owner's invitation took effect on September 11. Both accounts now have
effective project ADMIN access through organization `org-steep-voice-65223981`.
The connected API can read the existing project, its members, branches, and SQL.
The invitation/account blocker described above is resolved.

- Production branch: `br-patient-mouse-am8nt5kq`.
- Fresh isolated branch: `br-proud-lake-amjmp0ki`, named
  `q109-verification-20260911`; its parent is production.
- Verified active template: `cmngz7mhr0000k8s6792itaz4`, Pool/Spa Inspection.
  All 64 existing jobs use it. Production has 110 fields and no Q109.
- Ran the actual backup/CAS updater on the isolated branch: 110 -> 111 fields.
  A second read-only run reports `changed: false`.
- Original fields hash `9337fdad2c72189e10095cc5afde3ad9` is unchanged after
  removing only the appended field from the isolated copy. Both inactive
  templates also retain their original hashes.
- New opt-in integration test uses the real Prisma Neon adapter and application
  actions. It passed concurrent Q107/Q109/autosave persistence, reopen, reorder,
  foreign-photo rejection, photo exclusion/removal, PDF rendering, email HTML,
  blank-section omission, and submitted-job edit rejection.
- The synthetic job was deleted; a digest check verifies that all 64 original
  jobs are unchanged. No email was sent. Cache invalidation and Blob deletion
  are mocked; photo fixtures are local image data, not real Blob uploads.
- TypeScript and ESLint pass with the integration test included. The previously
  reviewed Vercel preview remains READY at commit
  `349f094ea4036c9ce622d6056d2966c3f35e6418`.
- Hosted preview access returns the app's existing Basic-auth 401. The cloud
  browser cannot open the local development server (`ERR_BLOCKED_BY_CLIENT`).
  No browser walkthrough of a deployed form or real Blob upload has been
  verified. Preserve these limits in the release report; do not describe the
  automated database proof as a hosted end-to-end browser test.

Production is still unchanged at this checkpoint. The owner has already
authorized release after testing and another check after release. The remaining
release work is the GitHub merge/deployment, backed-up targeted production
template update, and production database/PDF checks. Hosted form testing still
requires the app's existing login; do not weaken it.

## Template update

Run from `pool-app` with the verified target environment configured:

```sh
node --import tsx scripts/add-reinspection-summary.ts --target VERIFIED_TEMPLATE_ID
node --import tsx scripts/add-reinspection-summary.ts --target VERIFIED_TEMPLATE_ID --apply --backup-dir PRIVATE_BACKUP_DIRECTORY
```

The first command is read-only. The apply command writes a private JSON backup
before changing the one selected template. It compares the original fields in
the update condition, refusing to overwrite concurrent edits. It preserves every
existing field and appends one optional textarea. Re-running is a no-op; conflicting
Q109 definitions or an unexpected template are rejected. Keep the backup outside
version control and retain it before making the production change.
