# 260417-rm2 — Legacy photo binding + remaining PDF divider

## Context for the fresh instance

You are executing the follow-up to task **260417-do8**, which shipped 5 commits (`1f170af`, `07ed2ea`, `c7dc648`, `6dd72cc`, `1ce60a8`) — reopen workflow + photo/question binding + first divider removal + atomicity + mobile wrap.

After deploy, the customer reported **two issues** that survived the first task:

- **Bug A (photos):** On already-submitted legacy jobs, photos are no longer rendering under their correct questions — they all pile up at the end under Q108 "Additional Photos." The previous commit `07ed2ea` removed the `photosQueue.shift()` fallback, which broke legacy jobs whose photo fields store **filenames** (not URLs). Codex flagged this as finding #3 in the first review; PLAN.md called it "acceptable trade-off" — but the customer says it's not acceptable. Fix it.

- **Bug B (divider):** A horizontal divider line still renders between the disclaimer block and the "Name / Submitted by / Date" section. The commit `c7dc648` removed **one** divider (the one between contact block and disclaimer) — but codex's first review noted "the remaining divider is still accounted for at generate-pdf.ts:131." That remaining divider is what the customer is actually referring to. Remove it.

## Base state

- HEAD: `1ce60a8` on `origin/main` (Vercel auto-deploys)
- Only these files to touch:
  - `src/lib/actions/generate-pdf.ts` — both fixes
- **Do NOT touch:**
  - `src/lib/actions/photos.ts` (has unrelated uncommitted atomic-jsonb work)
  - `.claude/settings.local.json`
  - Any file outside `pool-app/`
  - The other 5 committed files — they're correct as-is

## Read first

Before editing, **Read** `src/lib/actions/generate-pdf.ts` in full. The line numbers below are approximate since the file has shifted — verify by pattern match, not by line.

## Task 1 — Bug B: Remove the remaining divider line

**Find** the second divider block. Look for a second occurrence of the jsPDF divider pattern — it's immediately after the disclaimer text block and before the "Name / Submitted by" header. The signature is the same as the first one we already removed:

```ts
doc.setDrawColor(...);
doc.setLineWidth(...);
doc.line(MARGIN_X, y, PAGE_WIDTH - MARGIN_X, y);
y += <small number>;
```

**Delete the entire block** (including the `y +=` that follows, to keep y-accounting correct). If deleting leaves back-to-back blank lines, collapse them.

**Verify** by reading 10 lines before and 10 after the deletion to confirm the spacing into the next section (name/submitted-by) looks right.

**Commit:**
```
fix(260417-rm2): remove remaining PDF divider below disclaimer

The first divider (between contact block and disclaimer) was removed
in c7dc648. Customer's "line beside the logo" complaint referred to
the second divider (between disclaimer and name/submitted-by header).
Remove it to match the intended clean header.
```

## Task 2 — Bug A: Match legacy filename values back to their photo slot

**Read** the relevant regions:

1. Around `formDataPhotoUrls` construction — it currently filters `formData` values by `startsWith("http")`. This only catches URL-valued fields. Extend it so it **also catches filename-valued fields** by looking up the matching `PhotoMetadata` in `photos` by `filename === rawValue` and mapping to that photo's URL.

2. Around the non-Q108 photo field render block (where `rawValue` is checked for `http` before embedding). Reuse the same resolution: if `rawValue` is a string and not a URL, find `photos.find(p => p.filename === rawValue)?.url` and render that URL.

3. Ensure matched URLs are removed from `photosQueue` so they don't double-render under Q108.

**Exact behavior after fix:**
- Photo field with value = URL → renders under its question ✅ (already works)
- Photo field with value = filename → **resolve to matching photo URL, render under its question** ← NEW
- Photo field with value = undefined/null/"" → render "—" ✅ (already works)
- Photos not referenced by any field → drain under Q108 ✅ (already works)

**Edge cases:**
- A filename value that has no matching photo in `photos` (rare orphan) → render "—" (not "not found" text; fall through to the existing empty-state path)
- Multiple fields referencing the same filename → first match consumes the photo from the queue, subsequent matches render "—" (or resolve to same URL — either is defensible, pick the simpler implementation)

**Commit:**
```
fix(260417-rm2): resolve legacy filename-valued photo fields to their URLs

Pre-URL-migration jobs stored photo field values as filenames. After
07ed2ea removed the queue fallback, those fields lost their per-question
placement and all photos drained into Q108.

Now: for any non-URL photo field value, look up the matching
PhotoMetadata by filename and render that photo's URL under the
correct question. Matched photos are removed from the queue so they
don't double-render under Q108.

Codex finding #3 from the 260417-do8 review.
```

## Proof plan

After both commits pushed:

1. Vercel auto-deploys. Wait for "Ready" on dashboard.
2. Ask Alex to download the PDF for one of the two legacy submitted jobs (`Doug Domeier` or similar) and visually verify:
   - No divider line between disclaimer and "Name / Submitted by" header
   - Photos for questions like Q5 (Picture of Pool and Spa), Q16 (Photo of pool pump), Q25 (Picture of Cartridge) now render **under those questions**, not at the bottom
   - Q108 Additional Photos only contains photos that weren't referenced by any field

## Typecheck + codex

After each commit:
- `npx tsc --noEmit` — must be exit 0
- Run `codex exec --skip-git-repo-check -m gpt-5.4 --config model_reasoning_effort="high" --sandbox read-only -C "C:/Users/renea/lucac-vault-server/pool-app" "<review prompt>" 2>/dev/null`

## Gotchas

- The file has shifted since earlier conversation — **verify line numbers by pattern match**, not by memorized offsets.
- Don't re-introduce the `photosQueue.shift()` fallback. That was the old bug. The new behavior is **deterministic resolution by filename**, not arbitrary queue consumption.
- `PhotoMetadata` type import: it's already imported in the file — don't re-import.
- Legacy filenames may or may not match exactly (case sensitivity, whitespace). Use exact string equality unless you find evidence of normalization elsewhere.
